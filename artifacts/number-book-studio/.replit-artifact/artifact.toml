kind = "web"
previewPath = "/"
title = "Number Book Studio"
version = "1.0.0"
id = "artifacts/number-book-studio"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25593

[services.development]
run = "pnpm --filter @workspace/number-book-studio run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/number-book-studio", "run", "build" ]
publicDir = "artifacts/number-book-studio/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25593"
BASE_PATH = "/"
